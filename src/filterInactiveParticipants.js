const MS_TIME = 15 * 1000;

export default async function filterInactiveParticipants(participants) {
    const intervalID = setInterval(() => {
        // mongoDB filter with query selectors
    }, MS_TIME);
}